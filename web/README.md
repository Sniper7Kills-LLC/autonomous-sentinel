# `@autonomous-sentinel/web`

Next.js + React PWA for [Autonomous Sentinel](../README.md).

## Develop

```bash
npm run web:dev      # from monorepo root
# or
npm run dev          # from this directory
```

Reads `amplify_outputs.json` from `../amplify/` once a sandbox or deploy has produced it.
