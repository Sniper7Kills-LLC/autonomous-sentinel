{
  "name": "autonomous-sentinel",
  "version": "0.0.0",
  "private": true,
  "description": "Autonomous Sentinel — EAM Watch v4.0 monorepo (web, amplify backend, upload client).",
  "license": "Apache-2.0",
  "author": "Sniper7Kills LLC",
  "repository": {
    "type": "git",
    "url": "https://github.com/Sniper7Kills-LLC/autonomous-sentinel.git"
  },
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "web",
    "amplify",
    "upload-client"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test:tooling": "node --test 'lint-staged.config.test.mjs'",
    "amplify:sandbox": "npm run sandbox --workspace amplify",
    "web:dev": "npm run dev --workspace web",
    "client:dev": "npm run dev --workspace upload-client",
    "prepare": "husky"
  },
  "devDependencies": {
    "@eslint-react/eslint-plugin": "^5.10.0",
    "@eslint/js": "^10.0.1",
    "@next/eslint-plugin-next": "^16.2.9",
    "@types/node": "^25.9.2",
    "@vitest/coverage-v8": "^4.1.9",
    "eslint": "^10.6.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.7.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8",
    "prettier": "^3.9.3",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.62.1",
    "vite": "^8.1.0",
    "vitest": "^4.1.7"
  },
  "overrides": {
    "react": "19.2.7",
    "react-dom": "19.2.7"
  }
}
